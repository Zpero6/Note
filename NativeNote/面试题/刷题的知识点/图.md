## 图的代码实现

1. 邻接表
2. 邻接矩阵

邻接表是一个数组, 数组的每一个元素存储 多个节点的连通情况 . 比如 graph[1] 代表 id 为1 的节点指向的节点

当然也可以是Map ,用 键来存储id , 用值来存储节点的联通情况 , ==用HashMap 可以规避创建数组要预设大小的问题==

邻接矩阵是一个简单的二维数组, 方便矩阵计算

## 图的遍历

图的遍历和多差树的遍历类似 , 无非多加一个visited 数组 保证不会重复遍历






