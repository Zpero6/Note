1. 哈希表的底层是一个数组

 2. 哈希表依赖于哈希函数根据key 计算出一个索引值 ,这个索引是底层hash table 数组的一个索引值

哈希函数的复杂度是O(1) ,那么整体的复杂度就是o1 

如果用可变的ArrayList作为key,那么就会导致计算出来的索引可能改变,索引值是根据里面的元素计算出来的
一旦元素改变就会改变索引,导致哈希冲突或者错误索引

索引值到table 要经过环形数组变换,和size取余得到真正的索引


