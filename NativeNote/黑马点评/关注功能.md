## 关注功能

mysql 中创建一张表 follow ， 存关注关系： 关注者和被关注着 ， 关注时间

### 需求

1.点击关注实现关注

2.在共同关注中获得表中两条数据的共同关注着



#### 1.点击关注实现关注

关注：/follow/{id}/{isFollow}

isFollow == true：

创建Follow对象，填入属性id，follow——user——id

isFollow == false：

remove（new QueryWrapper<Follow> ().eq("user_id",userId).eq("follow_user_id",followUserId)）;



确定关注关系：/follow/or/not/{followerId}

return   Result . ok(query().eq(userId).eq(followId).count()>0);



